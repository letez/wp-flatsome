{
  "name": "wp-flatsome",
  "version": "1.0.0",
  "license": "MIT",
  "engines": {
    "node": ">=0.5.0"
  },
  "scripts": {
    "watch": "node node_modules/less-watch-compiler",
    "git-ignore": "git update-index --assume-unchanged '.remote-sync.json'; git update-index --assume-unchanged 'less/_variables.less'; git update-index --assume-unchanged 'less/theme.less'; git update-index --assume-unchanged 'less/theme.less'; git update-index --assume-unchanged 'flatsome-child/theme.php'"
  },
  "dependencies": {
    "less": "^3.9.0",
    "less-watch-compiler": "^1.13.0"
  }
}
